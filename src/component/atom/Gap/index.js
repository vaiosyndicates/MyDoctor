import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Gap = ({acc}) => {
  return <View style={{acc}} />;
};

export default Gap;

const styles = StyleSheet.create({});
